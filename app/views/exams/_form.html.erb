<%= form_with(model: exam) do |form| %>
  <% if exam.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(exam.errors.count, "error") %> prohibited this exam from being saved:</h2>

      <ul>
        <% exam.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nama, style: "display: block" %>
    <%= form.text_field :nama %>
  </div>

  <div>
    <%= form.label :alamat, style: "display: block" %>
    <%= form.text_field :alamat %>
  </div>

  <div>
    <%= form.label :profesi, style: "display: block" %>
    <%= form.text_field :profesi %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
